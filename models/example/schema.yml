
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
    - name: STG_EMP
      description: "{{ doc('rev_history') }}"   #call the STG_EMP_DOC.md
      columns:
          - name: EMPLOYEE_ID
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
         