version: 2

models:
 - name: STG_PRODUCT
   description: "This will store product information"
   #tests:
   # - dbt_expectations.expect_table_aggregation_to_equal_other_table:
   # expression: sum(amount)
   #   compare_model: ref("TRG_Product")
   #   group_by: [amount]
   
      